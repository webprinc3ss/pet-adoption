import React from 'react';

const Login = () => {
    return (
        <section>
            <h2>Log in or Sign up</h2>
            <p>Form goes here or it can be a modal as in Mern Book Stack project. But it is here for now. </p>
        </section>
    );
};

export default Login;